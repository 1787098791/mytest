<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.freedom.mytest.provider.mapper.UserInfoMapper">

<select id="selectUserInfoById" resultType="com.freedom.mytest.hello.common.UserInfo" parameterType="int">
    select * from testuserinfo where id>#{id}
</select>
</mapper>